# Meta 练习生成侧 QA（Batch 8，交叉复核前快照）

## 结论

- 候选文件：`regen/batch8_meta_exercises.json`
- 候选数量：80 条
- 数据范围：EP42，原练习 `sort_order` 215–330
- 首 ID：`re-zero-ex-ep042-215`
- 末 ID：`re-zero-ex-ep042-330`
- 练习类型：80/80 为 `sentence_understanding`
- 生成方式：逐条读取原 `learning_exercises`、对应 `learning_sentences`、当前 `subtitle_lines` 连续话轮及本地 Netflix 日文 CC 后，人工撰写 `prompt`、`answer`、`hint`、`review_note`
- 程序用途：仅用于只读选集、ID 与来源映射、JSON 结构、重复、连续片段、提示泄漏、validator 和哈希校验；没有用循环、Python 模板或其他程序生成、扩写教学文本
- 数据库操作：仅执行只读 SQL，写入 0

本批原答案 80/80 均为同一占位文案：

```text
用于本集慢读跟读的完整台词。
```

原提示也统一为 `V10慢读：先听情绪，再确认句意。`，不能独立支持学习。候选把这些记录改写为可作答的日语理解题，覆盖幼年爱蜜莉雅偷跑并发现封印、佛尔特娜与裘斯谈话、昴的“深夜情书”插曲、席玛要继续昨夜谈话、爱蜜莉雅与裘斯初次见面、爱蜜莉雅重新面对试炼，以及微精灵和精灵术师术语说明。

## 选集、备份与当前状态

只读选集严格采用当前剩余集合，没有额外 `offset`，也没有另加 manifest 跳过：

```sql
select e.*
from public.learning_exercises e
where e.answer like '用于%'
order by e.episode, e.sort_order, e.id
limit 80;
```

复算结果：

- 数据库当前选集：80 条，唯一 ID 80 个。
- 本地候选：80 条，唯一 ID 80 个。
- 位置级 ID 匹配：80/80。
- 全部为 EP42；`sort_order` 最小 215，最大 330。
- 备份表 `maintenance.regen_20260727_learning_exercises_backup` 存在：80/80。
- 当前 `prompt`、`answer`、`hint` 与备份逐字段一致：80/80。
- 当前答案仍为 `answer like '用于%'`：80/80；不同占位答案值仅 1 个。
- manifest 记录重叠：0；已应用 manifest 重叠：0。
- 以逗号连接顺序 ID 的 MD5：
  - 数据库：`70ec21cdb99404a571403e260827b10f`
  - 本地：`70ec21cdb99404a571403e260827b10f`
- 以 `|` 连接同一组顺序 ID 的辅助 MD5：`8ca0ad1ae9412a05b7f1bc3b37edccd8`。

## Learning sentence 与真实字幕

### 来源映射

- 80/80 条 `review_note` 含明确的 `learning_sentences` 来源 ID。
- 来源 ID 共 80 个且全部唯一。
- 数据库存在性：80/80。
- 来源 episode 与练习 episode 一致：80/80。
- 从原练习日文引号抽出的目标与所选 `learning_sentences.ja_text` 逐字匹配：80/80。
- 新 `prompt` 逐字包含原目标日文：80/80。
- 按练习顺序以逗号连接来源 ID 的 MD5：
  - 数据库：`55bd7398ab0d7c1f613669652a600216`
  - 本地：`55bd7398ab0d7c1f613669652a600216`
- 按同一顺序连接目标日文的 MD5：
  - 数据库：`c644e268cb859948d93d69e9cfe931c9`
  - 本地：`c644e268cb859948d93d69e9cfe931c9`

### 当前日语字幕

- 76/80 个目标日文在当前 `subtitle_lines.ja_text` 中逐字命中。
- 逐字命中字幕行共 76 行，没有目标歧义或多行重复。
- 去除空格、引用符号与跨行破折号后：80/80 命中，命中行 80 行。
- 4 条非逐字命中均只有字幕排版边界差异：
  1. `re-zero-ex-ep042-254`
     - source：`恋はリングドーンの 先人にならえってなあ`
     - current line 269：`“恋はリングドーンの 先人にならえ”ってなあ`
  2. `re-zero-ex-ep042-255`
     - source：`お前に そのつもりはなくても`
     - current line 270：末尾另有跨行破折号 `ーー`
  3. `re-zero-ex-ep042-264`
     - source：`言い直さなくっても`
     - current line 284：末尾另有跨行破折号 `ーー`
  4. `re-zero-ex-ep042-285`
     - source：`私は今… たった今`
     - current line 325：末尾另有跨行破折号 `ーー`

除数据库字幕外，还连续复查：

- EP42 当前字幕 lines 188–440。
- 本地 Netflix CC blocks 185–440；目标实际分布于 blocks 195–434。

Netflix CC 的说话人标注用于确认幼年爱蜜莉雅／成人爱蜜莉雅、亚齐、佛尔特娜、裘斯、昴、嘉飞尔、奥托、拉姆、席玛与艾姬多娜之间的换人边界。候选没有根据单行旧中文释义猜人物，也没有把相邻人物的话拼成同一人的句子。

### 重点证据边界

1. `re-zero-ex-ep042-215`、`246`、`281`、`326`、`327`
   - 「ダツゴク」「フーイン」「セツド／タイグー」「ビセーレー」「セーレージュツシ／テンピン」均结合成人原话或现场动作解释为儿童按声音记忆陌生词。
   - 没有把片假名机械教成外来语，也没有把儿童近似发音伪造为新的专有名词。
2. `re-zero-ex-ep042-248`、`252`、`263`
   - 当前字幕和 Netflix CC 均保留「いくらっなんでも」「落ち込みっすぎ」「心配っすんな」「どこっまで」等非常规促音。
   - 候选先还原标准结构，再把小「っ」作为角色用力、卡顿或转写层面的发音线索；没有推广成可套用语法。
3. `re-zero-ex-ep042-254`
   - Netflix CC 用引号圈出嘉飞尔的格言式引用。
   - 候选只把可验证的「先人にならえ」「って」按普通日语解释，没有声称「恋はリングドーンの」是现实通行谚语。
4. `re-zero-ex-ep042-255`、`264`、`269`、`285`、`292`
   - 分别把以「ても」、宾格「を」、时间短语或动作列举收尾的字幕与相邻行连读。
   - 没有把跨行前半伪装成独立完整命题。
5. `re-zero-ex-ep042-283`–`290`
   - 裘斯反复保证“大丈夫”后又因幸福落泪；候选区分“没有受伤／并非悲伤”和“情绪毫无波动”。
   - 爱蜜莉雅的重复安慰按互动行为解释，没有给第二遍杜撰新事实。
6. `re-zero-ex-ep042-292`、`293`、`297`、`298`
   - Netflix CC 明确标注说话人为艾姬多娜。
   - 候选结合后续「留飲を下げる」识别讥讽和敌意，没有把她的话教成中性建议。
7. `re-zero-ex-ep042-302`
   - 「走り回られては」按照看者受影响的被动与条件处理，同时区分动作实施者、受影响者和面临身体风险的人。
8. `re-zero-ex-ep042-328`、`330`
   - 「Aと呼ばれるB」按名称槽与中心名词拆分。
   - 「吹き込んじゃダメ」按观念灌输的比喻义和「ては→ちゃ」禁止形式解释。

## 人工原创与学习质量

- 题面自足：80/80。每条含目标日语、必要场景及明确可操作的问题。
- 答案针对性：80/80。每条回答真实词形、句法、语义、语用、说话人立场或前后照应问题。
- 提示独立性：80/80。提示只给观察路径，不复述完整答案。
- 学习者可见的 `prompt`、`answer`、`hint` 中，旧占位答案、旧 V10 提示、内部 source ID、UUID、review 定位、Netflix CC、模型或生成器信息泄漏：0。
- `answer` 与 `hint` 去除空格、标点后的最长连续公共片段：
  - 最大 6 字。
  - 达到 8 字提示泄漏阈值：0。
- 不可见格式字符：0。

## 唯一性、去模板与结构

本批内部精确重复：

| 字段 | 重复组 |
| --- | ---: |
| `prompt` | 0 |
| `answer` | 0 |
| `hint` | 0 |
| `review_note` | 0 |

与 Meta Batch 1–7 共 560 条既有候选相比：

| 字段 | 精确重复 |
| --- | ---: |
| `prompt` | 0 |
| `answer` | 0 |
| `hint` | 0 |
| `review_note` | 0 |

附加结果：

- 与 Batch 1–7 的 ID 重叠：0。
- Batch 1–8 合并：640 条，唯一 ID 640 个。
- 对 `prompt + answer + hint` 去除空格、标点后建立连续 20 字符窗口，涉及本批的跨卡复用组：0。
- 每条且仅含 `id`、`prompt`、`answer`、`hint`、`review_note` 五个非空字符串字段：80/80。
- 空字段：0。
- review 来源缺失：0；review 来源重复：0。

## Validator

本批：

```text
node regen\validate_candidates.mjs regen\batch8_meta_exercises.json
```

结果：

```json
{
  "files": 1,
  "rows": 80,
  "uniqueIds": 80,
  "errors": [],
  "warnings": []
}
```

Meta Batch 1–8 合并：

```json
{
  "files": 8,
  "rows": 640,
  "uniqueIds": 640,
  "errors": [],
  "warnings": []
}
```

## 文件哈希

`regen/batch8_meta_exercises.json`

```text
C9C425803A57B0F321953E6738E1DBF8AC6AC4886E67D738973B807E207205FE
```

## 状态

生成侧 QA 已通过，`regen/batch8_meta_exercises.json` 自上述校验与哈希起冻结，不再编辑。**可以释放给独立 reviewer。** 本轮没有执行任何数据库写入。
