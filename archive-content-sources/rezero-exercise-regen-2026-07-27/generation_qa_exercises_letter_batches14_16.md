# Letter-answer exercises batches 14–16：生成侧 QA

## 结论

本批从数据库当前未应用的单字母答案练习中，严格按 `episode, sort_order, id` 取下一组 120 条。每题均逐条读取原记录、相关学习项、真实日文字幕及上下文后人工重写 `prompt / answer / hint / review_note`。脚本仅用于选行、ID 映射、结构、重复、泄漏和哈希校验，没有生成教学文本；全程没有写数据库。

| 文件 | 条数 | 范围 |
|---|---:|---|
| `batch14_exercises_letter.json` | 40 | EP18 exercise 049–088 |
| `batch15_exercises_letter.json` | 40 | EP18 exercise 089–128 |
| `batch16_exercises_letter.json` | 40 | EP18 exercise 129–168 |
| **合计** | **120** | **EP18 exercise 049–168** |

## 选集冻结与精确映射

筛选条件：

1. `work_slug = 're-zero'`。
2. 原 `answer` 去空白后为单个 `A/B/C/D` 字母。
3. 排除 `maintenance.regen_20260727_manifest` 中同 ID 且 `applied_at is not null` 的练习。
4. 按 `episode, sort_order, id` 排序，取前 120。

最终只读复查结果：

- 数据库选集：120 条、120 个唯一 ID，全部属于 EP18。
- 候选文件：120 条、120 个唯一 ID。
- 数据库首尾：`re-zero-s01e18-exercise-049` → `re-zero-s01e18-exercise-168`。
- 候选首尾：`re-zero-s01e18-exercise-049` → `re-zero-s01e18-exercise-168`。
- 数据库有序 ID MD5（以 `|` 连接）：`396201f26c032d9979416362e4cb8714`。
- 候选有序 ID MD5（以 `|` 连接）：`396201f26c032d9979416362e4cb8714`。
- 逐位置 ID 差异：0。

## 日语与上下文证据

- 120 条原练习及其实际目标词形均逐条读取。
- 目标日语均在 EP18 真实字幕中定位，覆盖 lines 5–112；重写时同时连读上下话轮。
- 相关 `learning_sentences`、词汇项与语法项均独立核对，没有只凭原题标签作答。
- 读音题区分了“字幕中实际活用形”与“词典基本形”；语义题保留说话人、听者、施事、受事、因果和省略边界。
- 终检按日语拍重新校正了 `まじょ` 为两拍，并把 `どうしようもない` 精确切为 `ど・う・し・よ・う・も・な・い`；同时去除了把 `たび` 错说成浊音开头、把 `げい` 错说成临时浊化的表述。

### 明确修正的源题问题

- 多条读音提示只给基本形，本批改答字幕中真实词形，并另列基本形：
  - `果たされた` → `はたされた`
  - `尊き` → `とうとき`
  - `破った` → `やぶった`
  - `足りない` → `たりない`
  - `死なせた` → `しなせた`
  - `従い` → `したがい`
  - `疲れて` → `つかれて`
  - `焦って` → `あせって`
  - `悩み`（`悩みまくって`）→ `なやみ`
  - `丸く収め` → `まるくおさめ`
  - `逃げよう` → `にげよう`
- `exercise-105`、`exercise-106` 的源题写成命令形 `死ね`，真实字幕是 `死ぬほど憎んだ男の声`；候选改为 `死ぬ（しぬ）` 及程度构式 `死ぬほど`，并明确排除命令义。
- `exercise-137` 对照真实字幕 `諦め悪い`，区分表面读法 `あきらめわるい` 与完整常见形式 `諦めが悪い`，没有把未出现的 `が` 算入字幕读音。
- `exercise-161` 的源语法把 `黙って` 中的字面连续 `って` 误标为引用助词；候选按 `黙る` 的て形及 `〜ている／〜ていなよ` 重写。
- `exercise-168` 的源题把 `あってはならない` 误标为条件 `〜なら`；候选按完整禁止／不可接受构式 `Vてはならない` 重写。
- `死なせた` 按因果责任与失责解释，不武断补成亲手杀害；`呼び寄せられた` 的未明施事、`楽な旅路には…` 的未完谓语等均保留证据边界。

### 角色与指代核对

- `黙っていなよ スバル`、`本気で僕を殺したいなら`、`影を伸ばしてみせろ` 的说话人是帕克。
- `〜べきだった`、逆接 `〜ながら`、`なすべきこと`、`あってはならない` 的说话人是培提其乌斯。
- `君の罪は３つある` 后的三项过错由帕克对昴列举。
- `死ぬほど憎んだ男の声` 至 `怠惰だね スバル` 的上下文将声音识别为昴自己的自我责难，不误归给培提其乌斯。
- `俺と一緒に逃げよう` 将雷姆纳入共同提议；其后的西、北、南方帝国、怕冷及旅程未定均按连续话轮解释。

## 最终 QA

运行：

```text
node regen\validate_candidates.mjs regen\batch14_exercises_letter.json regen\batch15_exercises_letter.json regen\batch16_exercises_letter.json
```

结果：

- files：3
- rows：120
- unique IDs：120
- errors：0
- warnings：0

附加检查：

- 精确五字段 schema：120/120。
- 空字段：0。
- 单字母答案残留：0。
- 学习侧 `prompt / answer / hint` 中的选项、答案元话语、模板语言或内部 source ID 泄漏：0。
- 本批归一化重复 prompt：0。
- 本批归一化重复 answer：0。
- 本批归一化重复 hint：0。
- 本批归一化重复 prompt-answer：0。
- 与既有 24 个 exercise 候选文件、1,149 条记录的归一化 prompt 重复：0。
- 与既有候选的归一化 prompt-answer 重复：0。
- hint 完整包含 answer：0。
- answer 完整包含长度至少 8 的 hint：0。
- 归一化 answer-hint 最长公共连续片段达到 8 字符：0。

SHA-256：

- `batch14_exercises_letter.json`：`A06E890E6E022B5D5628E76DF20552761C096AED7B4B90DBEA180F5DAEB548F4`
- `batch15_exercises_letter.json`：`BA6DF37CA27A54236F98188C904C122953516245AF7C6F770297C9E922DEEEAB`
- `batch16_exercises_letter.json`：`448F7133D9D453385A9C66694D8CFEC12457A8CA1F6DD280AF0EE04443EAF756`

## 冻结与释放

上述三个候选文件及本报告现已完成生成侧 QA 并冻结释放给独立 reviewer。释放后生成者不再编辑这些文件，除非独立 reviewer 明确退回具体问题。本报告只代表生成侧校验，不代表交叉复核结论；数据库仍保持未写入状态。
