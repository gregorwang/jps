# v3 ViewModel / component requests

One file per task package (`A.md`, `B.md`, …) so parallel worktrees never conflict.
Format per request: what is missing, which screen needs it, proposed field/function, current workaround.
